node_modules
.next
out
build

# env — keep .env.example, never commit real secrets
.env
.env.local
.env*.local

# Local member session (access + refresh tokens) — never commit
.flynet-session.json

.DS_Store
*.tsbuildinfo
next-env.d.ts
